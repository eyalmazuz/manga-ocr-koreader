# chrome_lens_ocr ships Google's public Lens web-client identifier in this
# vendored upstream file. It is not a project or contributor credential.
paths-ignore:
  - "backend/vendor/chrome_lens_ocr-0.3.0/src/constants.rs"
